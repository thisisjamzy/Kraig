// Placeholder data for the Settings screen's reminders. The user's
// name/email come from the Firebase Auth session instead
// (src/logic/settings/useLogic.ts), never from a static placeholder.

export const INITIAL_REMINDER_TIMES = ['09:00', '20:00'];

export const DEFAULT_CURRENCY_CODE = 'XAF';
